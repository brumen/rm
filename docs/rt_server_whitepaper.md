# rt_server Whitepaper

The maintained version of this whitepaper is now the LaTeX document:

- [`rt_server_whitepaper.tex`](./rt_server_whitepaper.tex)

Build with:

```bash
pdflatex docs/rt_server_whitepaper.tex
```
